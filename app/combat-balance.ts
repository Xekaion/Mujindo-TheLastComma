/**
 * The unmodified player shot. Opening enemies have 24-28 HP, so this keeps
 * them at two normal hits instead of making the most common one take three.
 */
export const BASE_PLAYER_ATTACK_DAMAGE = 14;
